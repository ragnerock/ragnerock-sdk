# Doc update

- [ ] Make it clear that the `source_url` parameter of documents is _only_ for images
- [ ] Remove the LLM-ified parts of the docs
- [ ] Verify that annotation getting behavior/hydration operates as we would expect